export interface AddressFields {
    name: string;
    contact: string;
    pincode: string;
    locality: string;
    address: string;
    city: string;
    state: string;
    landmark?: string; // optional
  }